# PDF2ZH v{{version}}

- 发布产物：`DMG`、`App ZIP`
- 支持平台：仅 `macOS arm64`
- 当前能力：`PDF -> Markdown -> Typst -> PDF`，并已内置离线 runtime
- 使用提醒：当前版本未进行 Apple 签名与 notarization，首次打开时可能遇到 macOS 安全提示
- 已知限制：当前仅支持 macOS，且暂未做 Apple notarization
