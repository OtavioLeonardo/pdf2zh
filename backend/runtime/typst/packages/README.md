# Bundled Typst Packages

Vendored Typst packages should be copied here for offline use.

Suggested layout:

- `preview/cetz/<version>/`
- `preview/lilaq/<version>/`

Use `PDF2ZH_BUNDLE_TYPST_PACKAGES_DIR` with `npm run runtime:prepare` to populate this directory from a prepared local package cache.
