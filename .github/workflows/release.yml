name: release

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  macos:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - uses: dtolnay/rust-toolchain@stable
      - uses: astral-sh/setup-uv@v5
      - run: npm ci
      - name: Install runtime tools
        run: brew install pandoc tectonic
      - name: Prepare runtime
        run: |
          uv python install 3.12
          PYTHON_BIN="$(uv python find 3.12)"
          PYTHON_HOME="$(dirname "$(dirname "$PYTHON_BIN")")"
          export PDF2ZH_BUNDLE_PANDOC="$(command -v pandoc)"
          export PDF2ZH_BUNDLE_TECTONIC="$(command -v tectonic)"
          export PDF2ZH_BUNDLE_PYTHON_HOME="$PYTHON_HOME"
          export PDF2ZH_RUNTIME_PYTHON="$PYTHON_BIN"
          npm run runtime:prepare
      - name: Verify bundled runtime
        run: |
          test -f backend/runtime/bin/pandoc
          test -f backend/runtime/bin/tectonic
          test -x backend/runtime/bin/pandoc
          test -x backend/runtime/bin/tectonic
          BUNDLED_PYTHON="backend/runtime/python/bin/python3"
          if [ ! -x "$BUNDLED_PYTHON" ]; then
            BUNDLED_PYTHON="backend/runtime/python/bin/python"
          fi
          test -x "$BUNDLED_PYTHON"
          "$BUNDLED_PYTHON" --version
          backend/runtime/bin/pandoc --version | head -n 2
          backend/runtime/bin/tectonic --version
      - name: Build macOS app bundle
        run: npm run tauri build -- --bundles app
      - name: Runtime and app size summary
        run: |
          echo "Runtime size:"
          du -sh backend/runtime
          echo "App bundle size:"
          du -sh src-tauri/target/release/bundle/macos/PDF2ZH.app
      - name: Archive macOS app for release
        run: |
          ditto -c -k --sequesterRsrc --keepParent \
            src-tauri/target/release/bundle/macos/PDF2ZH.app \
            "src-tauri/target/release/bundle/macos/PDF2ZH_${GITHUB_REF_NAME}_macos_app.zip"
      - uses: actions/upload-artifact@v4
        with:
          name: pdf2zh-macos-app
          path: src-tauri/target/release/bundle/macos/PDF2ZH.app
      - name: Build macOS dmg
        continue-on-error: true
        run: npm run tauri build -- --bundles dmg
      - name: DMG size summary
        if: ${{ hashFiles('src-tauri/target/release/bundle/dmg/*.dmg') != '' }}
        run: du -sh src-tauri/target/release/bundle/dmg/*.dmg
      - uses: actions/upload-artifact@v4
        if: ${{ hashFiles('src-tauri/target/release/bundle/dmg/*.dmg') != '' }}
        with:
          name: pdf2zh-macos-dmg
          path: src-tauri/target/release/bundle/dmg/*.dmg
      - name: Upload macOS assets to GitHub Release
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if ! gh release view "${GITHUB_REF_NAME}" >/dev/null 2>&1; then
            gh release create "${GITHUB_REF_NAME}" --title "${GITHUB_REF_NAME}" --notes "" || \
              gh release view "${GITHUB_REF_NAME}" >/dev/null
          fi
          shopt -s nullglob
          files=(
            src-tauri/target/release/bundle/macos/PDF2ZH_${GITHUB_REF_NAME}_macos_app.zip
            src-tauri/target/release/bundle/dmg/*.dmg
          )
          gh release upload "${GITHUB_REF_NAME}" "${files[@]}" --clobber

  windows:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - uses: dtolnay/rust-toolchain@stable
      - uses: astral-sh/setup-uv@v5
      - run: npm ci
      - name: Install runtime tools
        run: choco install pandoc tectonic -y
      - name: Prepare runtime
        shell: pwsh
        run: |
          function Get-NonShimCommandPath([string]$name) {
            $sources = @(Get-Command $name -All | Select-Object -ExpandProperty Source -Unique)
            if ($sources.Count -eq 0) {
              throw "Could not resolve command path for $name."
            }

            $shimRoot = if ($env:ChocolateyInstall) {
              Join-Path $env:ChocolateyInstall "bin"
            } else {
              $null
            }

            $preferred = $sources | Where-Object {
              -not $shimRoot -or -not $_.StartsWith($shimRoot, [System.StringComparison]::OrdinalIgnoreCase)
            } | Select-Object -First 1

            if ($preferred) {
              return $preferred
            }

            $libRoot = if ($env:ChocolateyInstall) {
              Join-Path $env:ChocolateyInstall "lib"
            } else {
              $null
            }

            if ($libRoot -and (Test-Path $libRoot)) {
              $candidates = Get-ChildItem -Path $libRoot -Recurse -Filter $name -File -ErrorAction SilentlyContinue |
                Where-Object {
                  -not $shimRoot -or -not $_.FullName.StartsWith($shimRoot, [System.StringComparison]::OrdinalIgnoreCase)
                } |
                Sort-Object FullName

              if ($candidates.Count -gt 0) {
                return $candidates[0].FullName
              }
            }

            throw "Resolved only Chocolatey shim paths for ${name}: $($sources -join ', ')"
          }

          uv python install 3.12
          $pythonBin = uv python find 3.12
          $pythonHome = Split-Path -Parent $pythonBin
          $env:PDF2ZH_BUNDLE_PANDOC = Get-NonShimCommandPath "pandoc.exe"
          $env:PDF2ZH_BUNDLE_TECTONIC = Get-NonShimCommandPath "tectonic.exe"
          $env:PDF2ZH_BUNDLE_PYTHON_HOME = $pythonHome
          $env:PDF2ZH_RUNTIME_PYTHON = $pythonBin
          npm run runtime:prepare
      - name: Verify bundled runtime
        shell: pwsh
        run: |
          $runtimeBin = Join-Path $PWD "backend/runtime/bin"
          $runtimePython = Join-Path $PWD "backend/runtime/python"
          $pandoc = Join-Path $runtimeBin "pandoc.exe"
          $tectonic = Join-Path $runtimeBin "tectonic.exe"

          if (!(Test-Path $pandoc)) {
            throw "Bundled pandoc.exe was not prepared."
          }
          if (!(Test-Path $tectonic)) {
            throw "Bundled tectonic.exe was not prepared."
          }

          $pythonCandidates = @(
            (Join-Path $runtimePython "python.exe"),
            (Join-Path $runtimePython "python3.exe"),
            (Join-Path $runtimePython "bin/python.exe"),
            (Join-Path $runtimePython "bin/python3.exe")
          ) | Where-Object { Test-Path $_ }

          if ($pythonCandidates.Count -eq 0) {
            throw "Bundled Python interpreter was not prepared."
          }

          $bundledPython = $pythonCandidates[0]

          & $bundledPython --version
          & $pandoc --version | Select-Object -First 2
          & $tectonic --version
      - name: Build
        run: npm run tauri build
      - name: Bundle size summary
        shell: pwsh
        run: |
          Write-Host "Runtime size:"
          Get-ChildItem backend/runtime -Recurse | Measure-Object -Property Length -Sum
          Write-Host "Bundle outputs:"
          Get-ChildItem src-tauri/target/release/bundle -Recurse -File | Select-Object FullName,Length
      - uses: actions/upload-artifact@v4
        with:
          name: pdf2zh-windows
          path: src-tauri/target/release/bundle/**
      - name: Upload Windows assets to GitHub Release
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release view $env:GITHUB_REF_NAME *> $null
          if ($LASTEXITCODE -ne 0) {
            gh release create $env:GITHUB_REF_NAME --title $env:GITHUB_REF_NAME --notes "" *> $null
            if ($LASTEXITCODE -ne 0) {
              gh release view $env:GITHUB_REF_NAME *> $null
            }
          }

          $files = Get-ChildItem src-tauri/target/release/bundle -Recurse -File |
            Where-Object { $_.Extension -in @('.msi', '.exe') } |
            Select-Object -ExpandProperty FullName

          if ($files.Count -gt 0) {
            gh release upload $env:GITHUB_REF_NAME $files --clobber
          } else {
            Write-Host "No Windows release assets found to upload."
          }
